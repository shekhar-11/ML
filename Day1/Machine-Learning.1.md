Machine learning -- where we give the machine some inputs and output sets and on the basis of these set , the machine predicts the pattern to tell 
the output for further unknown inputs


the input, output set provided is called training set

These are of 2 types-- supervised(labelled which is known), unsupervised


Supervised-- here the model is trained with data having some specifications
unsupervised -- model is just fed with the data and it comes up with some pattern


Supervised -- 2 types
Regression -- mapping of input to output 
it can have infinite values --
some functions can be generalised to give the output -- like mx+c or .....some curve
eg -- house price prediction

classification -- classifies into 2 or more class/category

draw a line in between the two types above this is type 1 below is type 2 ( for example -- for classification)
there can be more no. of classification but not many 

regression has unlimited ip/op




